export enum CellColor {
  WHITE = 'white',
  BLACK = 'black',
}

export enum FigureTeam {
  WHITE = 'white',
  BLACK = 'black',
}

export enum HighlightType {
  NONE = 'none',
  SELECTED = 'selected',
  DEFAULT_STEP = 'step',
  KILL_STEP = 'kill',
}

export enum FigureType {
  PAWN = 'pawn',
  KING = 'king',
  QUEEN = 'queen',
  KNIGHT = 'knight',
  BISHOP = 'bishop',
  ROOK = 'rook',
}
