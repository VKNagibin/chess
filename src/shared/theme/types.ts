export interface Theme {
  colors: {
    primary: string;
    primarySoft: string;
    secondary: string;
    darkTypography: string;
    brightTypography: string;
    border: string;
  };
  spacing: {
    xs: string;
    sm: string;
    md: string;
    lg: string;
    xl: string;
  };
  borderRadius: {
    sm: string;
    md: string;
    lg: string;
  };
}
